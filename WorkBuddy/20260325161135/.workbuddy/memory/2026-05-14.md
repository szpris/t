# 2026-05-14

## MQTT-Sub 功能实现（App 实际仓库）

将 MQTT-Sub 功能写入真实 GitHub 仓库目录 `C:\Users\pris\.qclaw\workspace\gps-tracker\android\`（包名 com.gpstracker.app），而非 WorkBuddy 工作区。
- 新建 `MqttManager.kt`：订阅 xy.nasi.cn:1883 topic=SM09aZ09aZ/GPSTrackHost/info，自动重连
- 修改 `LocationService.kt`：集成 MQTT，处理 now/start/stop 三条指令
- `app/build.gradle`：加 paho 1.2.5 依赖
- `build.gradle`（root）：allprojects.repositories 加 Eclipse paho maven 仓库
- `AndroidManifest.xml`：加 WAKE_LOCK 权限
用户用 `cd C:\Users\pris\.qclaw\workspace\gps-tracker\android && git add . && git commit && git push` 触发 GitHub Actions 编译
